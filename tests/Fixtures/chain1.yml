done: jobThree
lifetime: 86400
namespace: Tests\Fixtures

channels:
  - route: user.{user}
    visibility: private

jobs:
  jobOne:
    type: JobOne
    params:
      filePath: !param filePath
  jobTwo:
    type: JobTwo
    params:
      message: !job jobOne
  jobThree:
    type: JobThree
    params:
      message: !job jobTwo
